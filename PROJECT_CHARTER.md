# AI Context Linker 立项书

## 一句话目标

让用户在普通 ChatGPT 对话里获得持续、可信、低泄露风险的项目发展参谋体验，而无需上传整个项目或启动代码代理。

## 用户问题

ChatGPT 擅长讨论，但不了解本地项目的长期状态。Codex 和 Work 能读取更多工程材料，却不适合每一次方向讨论，也会把“聊项目”变成一次工程任务。直接上传仓库风险过高，GitHub 开源更不应成为获得讨论能力的前提。

## 产品承诺

AI Context Linker 只交付一层人工可审阅的项目认知面：

- 已确认事实；
- 当前目标与约束；
- 项目之间的关系；
- 最近变化、风险与未知；
- 希望 ChatGPT 协助思考的问题。

输出保持小、稳定、可追溯。任何推断都必须标记为推断；任何未知都不能伪装为事实。

## 非目标

- 不把源码、Git 历史或整个工作区搬到云端。
- 不替代 Codex 的代码级读取、修改、执行和测试。
- 不自动替用户决定项目优先级。
- 不将 Google Drive 或关系图当作事实真源。
- 不以 GitHub 开源为使用前提。

## 当前安全标准

1. 用户可从明确批准的 manifest 生成一份 ChatGPT 可读简报。
2. 默认输出中没有源码、本机绝对路径、密钥或私人运行数据。
3. 未知字段和危险内容必须拒绝生成，而不是静默忽略。
4. 项目关系可视为派生图谱，并能从 manifest 完整重建。
5. 同一输入生成稳定文件，适合放入专用 Google Drive 目录。

## 核心假设

经过筛选的高质量项目地图，比没有上下文的普通 Chat 更适合项目发展讨论，这一点已由公开 before/after 演示验证。是否能达到或超过 Work 的头脑风暴体验，仍需要用同一组真实问题持续对比，现阶段不把它写成事实。
